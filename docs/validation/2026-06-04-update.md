# 文档更新校验报告 (2026-06-04)

## 变更来源

本轮更新由 3 项代码变更驱动：
1. **按需指标计算** (executor.py): `_get_indicator_column()` 在预计算列查找失败时自动调用 `_compute_indicator()` 按需计算
2. **Train/Test 数据切分 + OOS 验证** (runner.py + db_ext.py): 70/30 时序切分，进化后 champion 样本外验证
3. **Profile 条件类型补充** (indicator_profile.py): 7 个趋势指标增加 cross_above_series/cross_below_series，BB 增加 lookback_any/lookback_all

## 受影响文档

| 文档 | 更新类型 | 更新内容 |
|------|----------|----------|
| domain/策略DNA与执行.md | 新增不变量 | #7 按需指标计算不变量 + 设计意图段落 |
| domain/特征工程.md | 新增不变量 | #7 高级条件推荐（cross_above_series/lookback_any 等）+ 更新设计意图 |
| domain/进化引擎.md | 新增不变量 | #10 按需指标计算 + #11 样本外验证 |
| flows/策略进化流程.md | 流程更新 | 添加 train/test split 步骤 + OOS 验证步骤 + 数据转换规则 |
| INDEX.yaml | 元数据更新 | 时间戳更新 |
| MAP.md | 元数据更新 | 时间戳更新 |

## 校验 A: 代码 <-> 文档一致性

### 策略DNA与执行.md

| 校验项 | 状态 | 说明 |
|--------|------|------|
| 不变量 #7 按需计算描述 | 通过 | executor.py:336-354 的 on-demand compute 逻辑与文档描述一致 |
| 设计意图段落 | 通过 | 文档准确描述了搜索空间扩展机制（~200 -> 数千参数组合） |

### 特征工程.md

| 校验项 | 状态 | 说明 |
|--------|------|------|
| 不变量 #7 条件推荐 | 通过 | indicator_profile.py 中 EMA/SMA/WMA/DEMA/TEMA/VWMA/Aroon 含 cross_above_series/cross_below_series，BB 含 lookback_any/lookback_all |
| PROFILES 覆盖数 | 通过 | 文档记录 40 个指标画像，代码中 PROFILES dict 含 40 条记录 |
| guard_only 标记 | 通过 | ATR/ADX/PSAR 标记为 guard_only，VWAP 未标记（已知限制，第六轮验证发现 #2） |

### 进化引擎.md

| 校验项 | 状态 | 说明 |
|--------|------|------|
| 不变量 #10 按需计算 | 通过 | 文档描述与 executor.py 实现一致 |
| 不变量 #11 OOS 验证 | 通过 | runner.py 中 OOS 验证块使用 test_df 评估 champion，结果写入 oos_fitness/oos_qualified/oos_metrics |
| db_ext.py 迁移 022 | 通过 | 3 个新列定义与文档描述一致 |

### 策略进化流程.md

| 校验项 | 状态 | 说明 |
|--------|------|------|
| Mermaid 序列图 | 通过 | 包含 _split_train_test 步骤和 OOS 验证 opt 块 |
| 数据转换规则表 | 通过 | 包含 _split_train_test 和 OOS 验证行 |
| 源码锚点 | 通过 | 新增 _split_train_test 和 OOS 验证块锚点 |

## 校验 B: 依赖模块引用准确性

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 进化引擎 -> 策略DNA | 通过 | 按需计算描述引用正确 |
| 进化引擎 -> 特征工程 | 通过 | Profile 条件类型引用正确 |
| 进化引擎 -> API接口层 | 通过 | OOS 验证流程引用正确 |
| API接口层 -> 持久化层 | 通过 | 迁移 022 新列引用正确 |

## 校验结论

全部 6 份更新文档通过代码 <-> 文档一致性校验。无不一致项。

### 已知限制（不在本次文档更新范围内）

- VWAP 缺 guard_only 标记（第六轮验证发现 #2），进化可能选为 trigger 导致 fitness=0
- 层内 momentum 覆盖问题（第六轮验证发现 #3），多 momentum 基因只保留最后一个
